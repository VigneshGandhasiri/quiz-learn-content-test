<img src="../../../CommonResources/Logo.MathCodeLab.Light.png" alt="MathCodeLab" width="200"/>

# Sorting: Practice Exercises ✏️

Practice implementing and analyzing sorting algorithms!

---

## 1. Bubble Sort

```python
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr

print(bubble_sort([64, 34, 25, 12, 22, 11, 90]))
```

---

## 2. Selection Sort

```python
def selection_sort(arr):
    for i in range(len(arr)):
        min_idx = i
        for j in range(i + 1, len(arr)):
            if arr[j] < arr[min_idx]:
                min_idx = j
        arr[i], arr[min_idx] = arr[min_idx], arr[i]
    return arr

print(selection_sort([29, 10, 14, 37, 13]))
```

---

## Practice Exercises

1. **Insertion Sort** — Implement insertion sort. Print the array after each insertion to visualize the process.

2. **Sort Strings** — Use any sorting algorithm to sort a list of names alphabetically.

3. **Swap Counter** — Modify bubble sort to count and print the number of swaps made.

4. **Sort by Length** — Sort a list of words by their length (shortest first). Use any algorithm.

5. **Nearly Sorted** — Given a nearly sorted array (each element is at most 2 positions from its sorted position), sort it efficiently.

---

## Challenge Exercises 🌟

6. **Counting Sort** — Implement counting sort for integers in the range 0-100.

7. **Visualize Sorts** — For a small array (size 8), run bubble, selection, and insertion sort. Print the array after each pass for each algorithm. Compare the number of comparisons and swaps.

---

© 2025 MathCodeLab Team. All Rights Reserved.
